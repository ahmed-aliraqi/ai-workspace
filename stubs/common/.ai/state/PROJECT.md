# {{PROJECT_NAME}}

> Installed {{DATE}} — stack: {{STACK_LABEL}} (`{{STACK_KEY}}`).
> Keep this file current: it is the first thing every session reads.

## Goal

<!-- One paragraph: what this project is and who uses it. -->
_TBD — fill this in before the first sprint._

## Modules

<!-- One line per module as they get built, e.g.:
- Auth: Sanctum sessions, roles via policies (app/Policies)
- Invoicing: app/Services/Invoicing, pages under resources/js/pages/invoices
-->

## Key architecture decisions

See `.ai/state/DECISIONS.md` for the log. Summarize only the load-bearing ones here.

## Environment

- Local dev: `composer run dev` (or `php artisan serve` + `npm run dev`)
- Tests: `php artisan test` (Pest) — formatting: `vendor/bin/pint --dirty`
- Database: _TBD_

## External services / integrations

_None yet._
